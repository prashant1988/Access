<!DOCTYPE html>
<html>
<head>
  <title>SpaceAccess</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>

 <style type="text/css">
      div.container
      {
          width:100%;
          margin:0px;
          border:1px solid gray;
          line-height:180%;
          background: #e8f4d4;
      }
      div.header,div.footer
      {
          padding:0.5em;
          color:white;
          background-color:#B2708F;
          clear:left;
          align: center;
      }
      h1.header
      {
          padding:0;
          margin:0;
      }
      div.left
      {
          float:left;
          width:180px;
          height: 540px;
          margin:0;
          padding:0;
          background: #e6e6fa;
      }
      div.content
      {
          /*border-left:2px solid gray;*/
          padding:0em;
      }

      div.r_content
      {
         margin: 0;
          padding: 0;

      }
  </style>
</head>
<body>

    <div class="container">

      <div class="header">
        <h2 class="header">
          <p>
            <% if user_signed_in? %>
                Signed in as <%= current_user.email %>
                <%= link_to "Sign out?", destroy_user_session_path, :method => :delete %>
            <% else %>
                <%= link_to "Sign up", new_user_registration_path %> or <%= link_to "sign in", new_user_session_path %>
            <% end %>
             <h1>Welcome In Domain_Access</h1>
          <% flash.each do |name, msg| %>
              <%= content_tag :div, msg, :id => "flash_#{name}" %>
          <% end %>
        </h2>
       </div>
       <div class="content">

          <%= yield %>

        </div>

      <div class="footer"><center>Copyright © , Relyon Softtech Limited</center></div>
    </div>
</body>
</html>
